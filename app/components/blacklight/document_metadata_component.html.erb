<dl class="document-metadata dl-invert row">
  <% @fields.each do |field| -%>
    <%= render((field.try(:component) || Blacklight::MetadataFieldComponent).new(metadata_field: field, show: @show)) %>
  <% end -%>
</dl>
